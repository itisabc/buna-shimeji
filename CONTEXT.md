# CONTEXT

このプロジェクト固有の用語と定義。実装の詳細・仕様・作業メモは書かない。

## マスコット
画面上を自律的に動く 1 体のしめじ。Rust 実装では `Mascot`。ユーザー操作を待たずに tick で状態が進む。

## アクション（Action）
マスコットの振る舞いの最小単位。`conf/actions.xml` の `<Action>` に対応する。
40 種を 1 つの enum + match で表し、Java 版のような継承階層は持たない。

## ビヘイビア（Behavior）
アクションの並びと発動頻度の定義。`conf/behaviors.xml` の `<Behavior>` に対応する。
マスコットは常にいずれか 1 つのビヘイビアを実行している。

## アフォーダンス（affordance）
個体同士が近づいた時に発動する相互作用（`Broadcast` / `ScanMove` 等）。
色を混ぜる（R22）もこの仕組みに乗せる想定。ビヘイビアとは別に、相手個体を必要とする。

## pin
マスコットをドロップしたウィンドウを最前面に固定する機能。
**ユーザーがドラッグしてドロップした時だけ**発動する（歩いて掴まった時は固定しない）。

## セル方式
ウィンドウを「最大フレーム寸法 + 余白 2M」で確保し、その中で sprite をずらして窓移動の回数を減らす描画方式。
#37 の初版で採用し、P0 ジッタ（再センター時に sprite が M px 逆行して見える現象）のため不採用。

## Tint
個体の色。**確定色**（色相・彩度・明度・グロー）+ 位相で表す。位相は `rotate` で進み、止まっていれば
確定色の色相をそのまま使う。**個体は色 id を持たない**（設定と結び付けるキーは set のパレット側にある）。

## パレット（`<TintPalette>`）
set ごとの「存在する色」の宣言（作者側）。1 色は `Id`（設定へ保存する安定キー）/ `Name`（表示名）/
`Hue` / `Sat` / `Lum` / `Glow`。**薄い色・濃い色・白・黒も 1 色**として書く（固定色はパレット 1 色で表す）。

## 気分（TintStyle）
set ごとの色の見せ方。色の選び方（`Off` / `Random` / `Cycle`）・回転速度・初期位相・
彩度・明度・グローを持つ宣言で、個体では変わらない。パステル / ゲーミングは set を分けて表現する。

## 許可色
「出現してよい色」の集合（set ごとの色 `Id`）。**お気に入り一覧とランダムの抽選母集団を兼ねる**
（別々には持たない）。色を集める図鑑ではない。`conf/settings.toml` の `[tint.sets.<set>] colors` に保存し、
**キー欠落 = 全色 / `colors = []` = 1 色も許可しない**を区別する。

## 劣化基準
移植の良し悪しの判定基準。「Java と一致するか」ではなく「**Java より劣化していないか**」で見る
（クラッシュ・ハング・復帰不能・観測できる不利益などの差異は修正する）。
